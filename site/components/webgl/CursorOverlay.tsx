"use client";

// TODO(webgl 负责人)：WebGL 光标覆盖层（public/model/cursor.glb + 拖尾 #c0fe04）。
// 此处仅为挂载点占位，保持 Provider 树结构与原站 RSC 布局一致；
// 实现时可读取 lib/pointer.tsx 的 usePointerContext()（uv Vector2 可每帧直读）。

export default function CursorOverlay() {
  return null;
}
